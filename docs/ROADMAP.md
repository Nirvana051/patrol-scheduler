# 未来规划（草案，收尾时定稿）

1. 真机联调：只读冒烟 → 单段下发 → 全流程；校准 FORWARD_DEG、settle、leg 超时。
2. 多机器人：robots 表 + 每机器人一组监听/轮询/执行线程；页面切换机器人。
3. 定时任务与队列；失败重试策略细化（避障失败 → 换路线）。
4. 全景 → 透视重投影后再给 VLM。
5. 检查结果统计与人工改判；报表导出。
6. 权限与审计。
